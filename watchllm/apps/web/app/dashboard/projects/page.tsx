"use client";
export const runtime = 'edge';
import { useState } from "react"; import useSWR from "swr"; import { useToast } from "@/components/ToastProvider"; import { apiGet, apiPost, type Project, type SimulationListPayload } from "@/lib/api";  export default function ProjectsPage() {   const { pushError } = useToast();   const projects = useSWR("/api/v1/projects", (url: string) => apiGet<Project[]>(url));   const simulations = useSWR("/api/v1/simulations?limit=100", (url: string) => apiGet<SimulationListPayload>(url));    const [open, setOpen] = useState(false);   const [name, setName] = useState("");    const createProject = async () => {     try {       await apiPost("/api/v1/projects", { name });       setOpen(false);       setName("");       await projects.mutate();     } catch (error) {       pushError(error instanceof Error ? error.message : "Unable to create project");     }   };    const rows = projects.data ?? [];    return (     <div>       <div className="mb-8 flex items-center justify-between">         <h1 className="text-2xl font-semibold text-white">Projects</h1>         <button type="button" onClick={() => setOpen(true)} className="rounded-lg bg-accent px-4 py-2 text-sm font-medium text-black">           New project         </button>       </div>        <div className="grid gap-4 md:grid-cols-3">         {rows.map((project) => {           const simulationCount =             simulations.data?.items.filter((sim) => sim.status && sim.user_id === project.user_id).length ?? 0;           return (             <div key={project.id} className="rounded-xl border border-white/5 bg-[#111] p-5">               <h2 className="mb-2 text-lg font-semibold">{project.name}</h2>               <p className="text-sm text-gray-400">Simulation count: {simulationCount}</p>               <p className="text-sm text-gray-500">Last run date: -</p>             </div>           );         })}       </div>        {open && (         <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4">           <div className="w-full max-w-md rounded-xl border border-white/10 bg-[#111] p-6">             <h2 className="mb-4 text-lg font-semibold">Create project</h2>             <input               value={name}               onChange={(event) => setName(event.target.value)}               placeholder="Project name"               className="w-full rounded border border-white/10 bg-black px-3 py-2 text-sm"             />             <div className="mt-5 flex justify-end gap-2">               <button type="button" onClick={() => setOpen(false)} className="rounded border border-white/10 px-3 py-2 text-xs">                 Cancel               </button>               <button type="button" onClick={createProject} className="rounded bg-accent px-3 py-2 text-xs font-medium text-black">                 Create               </button>             </div>           </div>         </div>       )}     </div>   ); }
